## 2026-08-28 - Added aria-labels to icon-only buttons
**Learning:** Found several icon-only buttons (like close buttons, remove buttons, voice input button) without `aria-label` attributes. Screen readers rely on these attributes to convey the purpose of icon-only interactive elements.
**Action:** Added descriptive `aria-label`s to all icon-only buttons across the app to improve accessibility for screen reader users.
